var Router = require('../')
require('chai').should()

describe('Router', function () {
    it('false test', function () {
    	console.log('bliblibli')
        true.should.be.equal(false);
    });
});